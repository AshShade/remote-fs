{
  "name": "remote-fs",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "trap 'kill 0' EXIT; bun --watch run server.ts 5657 & bun vite --config vite.config.ts",
    "build": "bun vite build --config vite.config.ts",
    "start": "bun run server.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitejs/plugin-vue": "5.2.3",
    "vite": "6.3.4",
    "vue": "3.5.13"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
